import React from 'react';
import Button from '@mui/material/Button';

const Buttons = () => {
  return (
    <div>
        <Button variant="text">Click Me</Button>
        <Button variant="contained">Submit</Button>
        <Button variant="outlined">Send Me</Button>
    </div>
  )
}

export default Buttons;